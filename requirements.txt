pandas
numpy
seaborn
matplotlib
scikit-learn
catboost
xgboost
dill
flask

#-e .   # this -e . means to install the current package in editable mode, which allows you to make changes to the code and have them reflected without needing to reinstall the package.
